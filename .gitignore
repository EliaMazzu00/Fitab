# Build output
bin/
obj/
build/

# Pacchetti dell'app: artefatti, non sorgenti (decine di MB l'uno).
# Git conserva ogni versione per sempre e i binari non si comprimono per
# differenze, quindi ogni build committata peserebbe sul clone di chiunque,
# per sempre. Vanno distribuiti dalle Release, non dal repository.
*.apk
*.aab
*.ipa

# Cartella di lavoro per l'estrazione dell'APK originale
extract/

# Note di reverse engineering sul backend e sull'app di Evin srl: documentano un
# servizio di terzi, credenziali comprese, e riportano codice estratto dall'app
# pubblicata. Il repository e' pubblico, quindi restano fuori.
API-FITAB.md
STRUTTURA-APP.md
PROPOSTA-2.0.md

# IDE
.vs/
.idea/
*.user
*.suo

# macOS
.DS_Store
